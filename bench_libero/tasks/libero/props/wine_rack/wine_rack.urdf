<?xml version="1.0"?>
<!-- GENERATED by tasks/libero/build_env/build_object_urdf.py. Do not hand edit.

     LIBERO object `wine_rack`, spawnable by the TRO-MP env.
     Role: static scene fixture (props/): furniture the tasks depend on, the
     support surfaces goals end on. Spawned KINEMATIC, so the mass and
     inertia below never act; they are computed the same way regardless
     rather than faked, in case it is ever spawned dynamic.

     ARTICULATION IS BAKED AWAY. Several of these have drawers, doors or
     knobs in LIBERO and are one rigid mesh here (props_registry.json
     flags `articulated_baked`). A task whose verb is `close the drawer`
     cannot be scored on this asset.

     WHY a URDF when envs/wine_rack/ may also hold a USD: this env spawns objects
     only through `assets.object_urdf` (scene_utils.py:1929, which calls
     _convert_urdf_to_usd at :1964). The assets dataclass declares no USD field,
     so a ready-made USD cannot be handed to this code path.

     Mesh coordinates are PRESERVED, not re-centred: 11 of 22 LIBERO envs have
     their mesh origin at the object's base, and `goal_traj` tracks that origin.
     Re-centring would offset the object from its own trajectory every frame.

     Mass: density 800.0 kg/m^3 x mesh volume 1.521199e-03 m^3
     Inertia: solid-box approximation about the COM at that mass, NOT the true
     mesh inertia tensor. Adequate for a graspable tabletop object; revisit if
     spin dynamics matter.

     Collision: the full mesh, decomposed by Isaac's URDF importer. The convex
     decomposition authored in the DexVerse USD is NOT reproduced here.
-->
<robot name="wine_rack">
  <link name="wine_rack">
    <inertial>
      <origin xyz="0.000726 -0.012850 0.100819" rpy="0 0 0"/>
      <mass value="1.216960"/>
      <inertia ixx="1.777352e-02" ixy="0" ixz="0" iyy="1.801121e-02" iyz="0" izz="1.454837e-02"/>
    </inertial>
    <visual>
      <origin xyz="0 0 0" rpy="0 0 0"/>
      <geometry><mesh filename="wine_rack.obj" scale="1 1 1"/></geometry>
    </visual>
    <collision>
      <origin xyz="0 0 0" rpy="0 0 0"/>
      <geometry><mesh filename="wine_rack.obj" scale="1 1 1"/></geometry>
    </collision>
  </link>
</robot>
